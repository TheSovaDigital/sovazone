name: Telegram v1 production review

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/telegram-production-review-v1.yml'

permissions:
  contents: read

jobs:
  review:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check Telegram production estimates
        run: |
          cat > /tmp/review.mjs <<'EOF'
          const endpoint='https://sovazone.vercel.app/api/telegram-username-value';
          const usernames=['qzxvna','hello_','aaaa','work','hello','alex','volk','denis','pavel','pasha','paris','money','cloud','dream','love','m4d','monkey','white','brand','trading','adidas'];
          let ok=0;
          for(const username of usernames){
            const r=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username,lang:'en',website:''})});
            const data=await r.json().catch(()=>({}));
            if(!r.ok)throw new Error(`${username}: HTTP ${r.status} ${JSON.stringify(data)}`);
            if(data.engineVersion!=='telegram-v1.0')throw new Error(`${username}: engine ${data.engineVersion}`);
            console.log(JSON.stringify({username,category:data.categoryCode,demand:data.demandScore,ton:[data.priceMinTon,data.priceMaxTon],usd:[data.priceMin,data.priceMax],liquidity:data.liquidity,special:data.specialCase}));
            ok++;
            await new Promise(r=>setTimeout(r,750));
          }
          console.log(`PASS ${ok}/${usernames.length} Telegram production responses`);
          EOF
          node /tmp/review.mjs
